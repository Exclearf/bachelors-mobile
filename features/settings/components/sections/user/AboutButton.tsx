import { StyleSheet, Text, View } from "react-native";
type Props = {};

const AboutButton = (props: Props) => {
  return (
    <View>
      <Text>Some text here about the project and that its for thesis.</Text>
    </View>
  );
};

export default AboutButton;

const styles = StyleSheet.create({});
