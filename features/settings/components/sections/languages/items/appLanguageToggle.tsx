import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const AppLanguageToggle = (props: Props) => {
  return (
    <View>
      <Text>AppLanguageToggle</Text>
    </View>
  );
};

export default AppLanguageToggle;

const styles = StyleSheet.create({});
