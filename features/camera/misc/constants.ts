import { PhotoQualityBalance } from "../stores/useCameraOptionsStore";

export const HANDLE_SIZE = 60;
export const HANDLE_HALF_SIZE = HANDLE_SIZE / 2;
export const HANDLE_THIRD_SIZE = HANDLE_SIZE / 3;
export const HANDLE_TWO_THIRD_SIZE = (HANDLE_SIZE * 2) / 3;

export const PhotoQualityBalanceValues: PhotoQualityBalance[] = [
  "speed",
  "balanced",
  "quality",
];
