import { View, Text } from "react-native";
import React from "react";
import { TouchableWithoutFeedback } from "react-native-gesture-handler";

type Props = {};

const ExpandableModalHeader = (props: Props) => {
  reurn(<TouchableWithoutFeedback></TouchableWithoutFeedback>);
};

export default ExpandableModalHeader;
